<!DOCTYPE html>
<html>
  <head>
    <title>Saasapp</title>
    <%= csrf_meta_tags %>
    <!-- Remove https:// from embedded font below for more flexibility, in case the website doesn´t get a SSL certificate -->
    <link href="//fonts.googleapis.com/css?family=Maven+Pro:400,900" rel="stylesheet">
    <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_include_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>

  <body>
    <nav class="navbar navbar-default navbar-static-top" role="navigation">
      <div class="container">
        <!-- Brand and toggle get grouped for better mobile display -->
        <div class="navbar-header">
          <button type="button" class="navbar-toggle" data-toggle="collapse" data-target="#main-nav-collapse">
            <span class="sr-only">Toggle navigation</span>
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
          </button>
        
        <!-- This is how links work in Ruby, and we can only use it in this html because of the file extension "erb"
        which stands for "Embedded Ruby" -->
          <%= link_to root_path, class: "navbar-brand" do %>
            <i class="fa fa-group"></i>
              DevMatch
            <% end %>
        
        </div>
        <div class="collapse navbar-collapse" id="main-nav-collapse">
          <ul class="nav navbar-nav navbar-right">
            <li><%= link_to "About", about_path %></li>
            <li><%= link_to "Contact Us", new_contact_path  %></li>
          </ul>
        </div> <!-- / .navbar collapse -->
      </div>
    </nav>
    
    <div class="container">
      <% flash.each do |type, msg| %>
        <%= content_tag :div, msg, class: "alert alert-#{type}" %>
      <% end  %>
    </div>
    
    <!-- yeld is where Rails pulls in the code from the other views files -->
    <%= yield %>
  </body>
</html>
